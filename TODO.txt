* Make 0 flag and precision together work as in C printf.
* Add "%v" formatting for all types to give a default format.
* Make sure the right things are exported for easy extension.
* Import the printf(3) manpage into the Haddock and edit it.
  per-type format.
* Fix RealFloat formatting to match printf standards
  (currently wontfix)
  * Fix standard floating formats
  * Implement "#" alt floating formats
* Deal with locale-specific "," and "." (deferred for now)
* Put in remaining checks for invalid formats? Don't know
  what is still missing here.
